# StudyMate QuizGenerator Frontend

## Development

1. `cd client`
2. `npm install`
3. `npm start`

- React frontend runs on port 3001 and proxies API requests to backend running on port 3000.
- Requires backend Express server (in `study_mate_quizgenerator/`) running.
